import 'package:flutter/material.dart';

class BuyTabContent extends StatelessWidget {
  const BuyTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('लेन Content'),
    );
  }
}
